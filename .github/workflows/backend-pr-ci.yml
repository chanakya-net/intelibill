name: Backend PR CI

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/backend-main-ci.yml
      - .github/workflows/backend-pr-ci.yml
      - src/backend/**
      - tests/backend/**
      - Directory.Build.props
      - Directory.Packages.props
      - global.json
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-test-backend:
    name: Build and Test Backend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Restore backend solution
        run: dotnet restore src/backend/Intelibill.slnx

      - name: Build backend solution
        run: dotnet build src/backend/Intelibill.slnx --configuration Release --no-restore

      - name: Run backend tests
        run: dotnet test src/backend/Intelibill.slnx --configuration Release --no-build --verbosity normal
